<%# locals: () -%>

<%= simple_form_for :search, url: search_iqdb_path do |f| %>
  <%= f.input :url, label: "URL", placeholder: "Also supports femboy fans post links" %>
  <%= f.input :file, as: :file, input_html: { accept: "image/*,video/*" } %>

  <%= f.submit "Search" %>
<% end %>
